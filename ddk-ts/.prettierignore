# Generated: ubrn writes src/ on every `pnpm generate`, and it runs prettier
# over its own output. Checking it here would only ever produce a loop — prettier
# rewrites a file that the next generation puts straight back. Same reason
# ddk-rn's eslintIgnore lists its generated files.
src

# Build output. tsc writes dist/ and build-release.mjs writes platform/; both
# are gitignored, and prettier was reporting them as failures.
dist
platform

# Lockfile: pnpm owns the formatting.
pnpm-lock.yaml
